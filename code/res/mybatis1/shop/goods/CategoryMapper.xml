<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CategoryMapper">

	<resultMap type="Menu" id="menuResultMap">
		<id column="MENU_ID" property="MENU_ID"/>
		<result column="MENU_NAME" property="MENU_NAME"/>
		<result column="MENU_URL" property="MENU_URL"/>
		<result column="PARENT_ID" property="PARENT_ID"/>
		<result column="MENU_ORDER" property="MENU_ORDER"/>
		<result column="MENU_ICON" property="MENU_ICON"/>
		<result column="MENU_TYPE" property="MENU_TYPE"/>
	</resultMap>

	<!-- ztree -->
	<resultMap type="Menu" id="menuZtreeResultMap">
		<id column="MENU_ID" property="MENU_ID"/>
		<result column="MENU_NAME" property="MENU_NAME"/>
		<result column="MENU_URL" property="MENU_URL"/>
		<result column="PARENT_ID" property="PARENT_ID"/>
		<result column="MENU_ORDER" property="MENU_ORDER"/>
		<result column="MENU_ICON" property="MENU_ICON"/>
		<result column="MENU_TYPE" property="MENU_TYPE"/>
		<result column="target" property="target"></result>
	</resultMap>
	
	<!--表名 -->
	<sql id="tableName">
		SHOP_CATEGORY
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		CATEGORY_NAME,	
		CATEGORY_EN_NAME,	
		CATEGORY_IMG,	
		SUPER_ID,	
		SORT,	
		CATEGORY_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{CATEGORY_NAME},	
		#{CATEGORY_EN_NAME},	
		#{CATEGORY_IMG},	
		#{SUPER_ID},	
		#{SORT},	
		#{CATEGORY_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			CATEGORY_ID = #{CATEGORY_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			CATEGORY_NAME = #{CATEGORY_NAME},
			CATEGORY_EN_NAME = #{CATEGORY_EN_NAME},
			CATEGORY_IMG = #{CATEGORY_IMG},
			SUPER_ID = #{SUPER_ID},
			SORT = #{SORT},
		CATEGORY_ID = CATEGORY_ID
		where 
		CATEGORY_ID = #{CATEGORY_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			CATEGORY_ID = #{CATEGORY_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		and
		SUPER_ID = #{pd.SUPER_ID}
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
					CATEGORY_NAME LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					CATEGORY_EN_NAME LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
				)
		</if>
		order by sort
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where SUPER_ID =#{SUPER_ID}
		order by sort
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			CATEGORY_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- da -->

	<!--通过ID获取其子一级菜单 -->
	<select id="listSubMenuByParentId" parameterType="String" resultMap="menuResultMap">
		select
           CATEGORY_ID  as MENU_ID,CATEGORY_NAME	as MENU_NAME,''	as MENU_URL,
 	       SUPER_ID	as PARENT_ID,''	as MENU_ICON,SORT	as MENU_ORDER,1	as MENU_STATE,1	as MENU_TYPE
        from shop_category
		where 1 = 1
		and SUPER_ID = CONCAT(#{parentId},'')
		order by SORT
	</select>


</mapper>